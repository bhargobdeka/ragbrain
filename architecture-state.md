# RAGBrain Architecture State

This file is auto-maintained by `ragbrain plan-upgrades`.
It tracks upgrade plans across runs so the planner builds on prior work rather than
repeating itself. Each run appends a new section with its date, summary, and recommendations.

**Do not delete this file** — it is the long-term memory for the self-improving loop.
You may add manual notes in the "Manual Notes" section below; the agent will read them.

---

## Manual Notes

Add any context here that you want the planner to always consider:

- Priority focus: retrieval quality and latency over throughput
- We are a single-developer open-source project — prefer low-effort, high-impact upgrades
- Avoid large infrastructure changes (e.g. moving off Qdrant) without strong justification
- Security hardening is deferred until before the first public release

---

## Run: Friday, March 27, 2026
### Summary
Today's Slack briefing (March 27, 2026) surfaced two distinct signals: a live supply-chain attack on LiteLLM (a direct dependency risk for any Python-based LLM project) and the first live agent-to-agent payment transactions by Santander, Mastercard, and Visa — a leading indicator that multi-agent coordination is moving from research to production. The knowledge base (currently only The RLHF Book) had no indexed material on any of these topics, confirming RAGBrain's knowledge corpus is thin on security and multi-agent literature. Cross-referencing against ARCHITECTURE.md and the (empty) prior-run state, three concrete upgrade recommendations emerge: (1) immediate dependency-pinning and supply-chain hardening triggered by the LiteLLM attack, (2) a multi-agent task-decomposition layer to close the known "no multi-agent coordination" gap, now validated as production-relevant by the payments news, and (3) ingesting dedicated RAG/retrieval security and multi-agent literature into the knowledge base to stop returning empty answers on these topics. No prior recommendations exist to carry forward.
### Recommendations
- [HIGH] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies (litellm, langchain, anthropic, qdrant-client, etc.) to exact versions in requirements.txt / pyproject.toml. Add pip-audit or safety to the CI pipeline to alert on newly published CVEs. Rotate any env vars, API keys, or secrets that were present in environments running LiteLLM 1.82.7–1.82.8. Consider using a private PyPI mirror or hash-verified installs (pip install --require-hashes) for the critical dependency set. *(Effort: DAYS)*
- [MEDIUM] **Agentic RAG — Multi-Agent Coordination**: Introduce a lightweight task-decomposition layer on top of the existing LangGraph StateGraph. A Planner agent breaks complex user queries into sub-tasks; existing CRAG/Self-Reflective RAG nodes handle each sub-task independently; a Synthesiser agent merges results. Start with a two-level hierarchy (Planner → Worker) using Claude 3.5 Haiku for planning to keep cost low. This directly closes the 'no multi-agent coordination / no task decomposition' known limitation. *(Effort: WEEKS)*
- [MEDIUM] **Knowledge Base / Ingestion**: Ingest a curated set of RAG, retrieval, and AI-security papers into the knowledge base so future architecture reviews return substantive answers rather than 'no information found'. Suggested seed list: (a) ColBERT / PLAID papers (late-interaction retrieval), (b) BEIR benchmark paper (retrieval evaluation), (c) GraphRAG / KG-RAG survey, (d) MLSecOps / AI supply-chain security literature (e.g. MITRE ATLAS). Use the existing RSS + WebExtractor pipeline to subscribe to arXiv cs.IR and cs.CR feeds. *(Effort: DAYS)*

### Already Covered
- ✓ Reciprocal Rank Fusion (RRF) for hybrid dense+sparse retrieval — already implemented
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ CrossEncoder reranking post-RRF — already implemented
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented
- ✓ AST-based code chunking via Tree-sitter — already implemented
- ✓ LangGraph StateGraph orchestration — already implemented

---

## Run: Tuesday, March 31, 2026
### Summary
Today's Slack briefing (March 30, 2026) surfaced two strong signals: (1) Salesforce's open-source VoiceAgentRAG, which demonstrates a Fast Talker / Slow Thinker dual-agent pattern over an async event bus with an in-memory FAISS semantic cache — claiming a 316x retrieval latency reduction — and (2) MCP crossing 97M installs and becoming de-facto agent infrastructure. Cross-referencing against ARCHITECTURE.md, RAGBrain has no semantic caching layer (every query hits Qdrant + BM25 cold), no streaming generation, and no MCP integration. The dual-agent latency pattern directly addresses RAGBrain's known "no streaming" and "no multi-agent coordination" gaps and is highly actionable given the open-source reference implementation. MCP adoption is now a low-effort, high-leverage integration given its 97M-install ubiquity. One prior HIGH recommendation (dependency pinning / supply-chain hardening) remains unconfirmed as completed and is carried forward as deferred.
### Recommendations
- [HIGH] **Retrieval**: Add an in-memory semantic cache layer (FAISS or Qdrant in-memory) in front of the full retrieval pipeline. On each query, embed the question and check cosine similarity against cached (query_embedding, result) pairs with a threshold (e.g. 0.92). On a cache hit, skip Qdrant + BM25 + RRF + CrossEncoder entirely and return the cached top-k. Evict by LRU with a configurable TTL. Model directly on Salesforce VoiceAgentRAG's FAISS semantic cache — the repo is open-source and available today. *(Effort: DAYS)*
- [HIGH] **Agentic RAG — Orchestration**: Introduce a Fast/Slow dual-agent split inside the existing LangGraph StateGraph. The 'Fast' path: semantic cache hit → direct answer (sub-200ms). The 'Slow' path: full CRAG + Self-Reflective RAG pipeline for cache misses or low-confidence fast answers. Wire them via an async event bus (asyncio.Queue or a lightweight pub/sub). This also naturally enables streaming: the Fast path can stream a partial/cached answer while the Slow path refines it in the background. Directly inspired by VoiceAgentRAG's open-source architecture. *(Effort: WEEKS)*
- [MEDIUM] **Ingestion / Integrations**: Add an MCP server adapter to RAGBrain's ingestion and query interfaces. Expose (a) the retrieval pipeline as an MCP tool (so any MCP-compatible agent/IDE can query RAGBrain's knowledge base), and (b) an MCP client inside RAGBrain's agentic loop so it can call external MCP servers (e.g. filesystem, GitHub, web search) as tools during CRAG query rewriting. Use the official Anthropic MCP Python SDK — it is already compatible with Claude Sonnet/Haiku which RAGBrain uses for generation. *(Effort: DAYS)*
- [MEDIUM] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies to exact versions in requirements.txt / pyproject.toml. Add pip-audit or safety to CI. Rotate any API keys that were present in environments running LiteLLM 1.82.7–1.82.8. Consider hash-verified installs (pip install --require-hashes) for the critical dependency set. *(Effort: DAYS)*

### Already Covered
- ✓ Reciprocal Rank Fusion (RRF) for hybrid dense+sparse retrieval — already implemented
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ CrossEncoder reranking post-RRF — already implemented
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented
- ✓ AST-based code chunking via Tree-sitter — already implemented
- ✓ LangGraph StateGraph orchestration — already implemented
- ✓ MCP conceptual coverage — already in knowledge base (The RLHF Book covers MCP as a JSON-RPC tool-use standard)

---

## Run: Tuesday, March 31, 2026
### Summary
Today's briefing (March 31, 2026) reinforces and extends the signals from March 30. Three new themes emerge: (1) Knowledge graphs are now cited as a production baseline alongside hybrid search — RAGBrain's known "no graph-based retrieval" gap is now a competitive deficit, not just a roadmap item. (2) Microsoft's vectorless reasoning approach (document structure + LLM reasoning, no vector DB) is a credible alternative retrieval path worth evaluating for document-heavy use cases. (3) The Microsoft Agent Framework (AutoGen + Semantic Kernel unified RC) with native MCP, graph workflows, A2A, and checkpointing signals that LangGraph may face a strong challenger — worth a strategic watch. Against prior runs: the semantic cache (HIGH, March 31) and MCP integration (MEDIUM, March 31) remain unconfirmed as implemented and are carried forward. Dependency pinning (HIGH, March 27 → MEDIUM, March 31) is still deferred per manual notes. The multi-agent coordination item from March 27 is now reinforced by the Microsoft framework signal. Given the single-developer constraint and the priority focus on retrieval quality and latency, the knowledge graph integration is the highest-value new recommendation this cycle.
### Recommendations
- [HIGH] **Retrieval**: Add an in-memory semantic cache (FAISS flat index) in front of the full retrieval pipeline. Embed each incoming query, check cosine similarity against cached (embedding → top-k results) pairs at a threshold of ~0.92, and short-circuit Qdrant + BM25 + RRF + CrossEncoder on a hit. Use LRU eviction with a configurable TTL (e.g. 1 hour). Reference implementation: Salesforce VoiceAgentRAG (open-source). This is the single highest-ROI latency win available today. *(Effort: DAYS)*
- [HIGH] **Retrieval — Knowledge Graph**: Introduce a lightweight knowledge graph layer alongside the existing vector + BM25 pipeline. Use spaCy or LLM-extracted entity/relation triples stored in NetworkX (in-process, no new infra). At query time, extract entities from the query, traverse 1–2 hops in the graph, and inject the resulting context chunks into the RRF candidate set before reranking. This closes the 'no graph-based retrieval' known limitation and aligns with the March 31 signal that KG integration is now a production baseline. *(Effort: WEEKS)*
- [HIGH] **Agentic RAG — Orchestration**: Introduce a Fast/Slow dual-agent split inside the existing LangGraph StateGraph. Fast path: semantic cache hit → stream answer directly (target <200ms). Slow path: full CRAG + Self-Reflective RAG for cache misses. Wire via asyncio.Queue. This simultaneously closes the 'no streaming' and 'no multi-agent coordination' known limitations and is directly modelled on the open-source VoiceAgentRAG architecture. *(Effort: WEEKS)*
- [MEDIUM] **Ingestion / Integrations — MCP**: Expose RAGBrain's retrieval pipeline as an MCP tool server using the official Anthropic MCP Python SDK. This allows any MCP-compatible agent, IDE (Cursor, VS Code Copilot), or external orchestrator to query RAGBrain's knowledge base as a first-class tool. Secondarily, add an MCP client inside the CRAG query-rewriting node so RAGBrain can call external MCP servers (web search, filesystem, GitHub) during retrieval augmentation. *(Effort: DAYS)*
- [MEDIUM] **Retrieval — Vectorless Reasoning**: Prototype Microsoft's vectorless reasoning approach as an optional retrieval mode for structured/document-heavy collections. Instead of chunking + embedding, pass document structure (headings, sections, tables) directly to Claude Haiku with a reasoning prompt to identify relevant passages. Benchmark against the current hybrid pipeline on a held-out document set. If it wins on explainability and relevance for structured docs (e.g. PDF books), offer it as a per-collection retrieval strategy. *(Effort: DAYS)*
- [LOW] **Agentic RAG — Framework Watch**: Track the Microsoft Agent Framework (unified AutoGen + Semantic Kernel RC) as a potential future replacement for LangGraph. Do not migrate now, but: (a) read the RC release notes and map its graph workflow primitives to RAGBrain's current StateGraph nodes, (b) note any features (checkpointing, human-in-the-loop, native A2A) that LangGraph lacks and that RAGBrain would benefit from, (c) set a decision checkpoint for Q3 2026 when the framework reaches stable release. *(Effort: DAYS)*
- [LOW] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies to exact versions in requirements.txt / pyproject.toml. Add pip-audit or safety to CI. Consider hash-verified installs (pip install --require-hashes) for the critical dependency set (anthropic, qdrant-client, langchain, sentence-transformers). *(Effort: DAYS)*

### Already Covered
- ✓ Hybrid dense+sparse retrieval (RRF over Qdrant + BM25) — already implemented, cited as production baseline in March 31 briefing
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented
- ✓ Cross-encoder reranking post-RRF — already implemented
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ AST-based code chunking via Tree-sitter — already implemented
- ✓ LangGraph StateGraph orchestration — already implemented
- ✓ MCP conceptual coverage in knowledge base (The RLHF Book)

---

## Run: Tuesday, March 31, 2026
### Summary
Today's Slack briefing (March 31, 2026) consolidates and reinforces signals from the past two days with no new breaking themes, but raises the urgency on three items that have now appeared across multiple consecutive runs without confirmed implementation. The dominant signals are: (1) "Naive RAG is officially dead" — knowledge graph integration is now cited as a production baseline alongside hybrid search, semantic chunking, and cross-encoder reranking; RAGBrain already covers the latter three but has no graph layer, making this a competitive gap. (2) Salesforce VoiceAgentRAG's open-source dual-agent (Fast/Slow) pattern with FAISS semantic cache claiming 316x latency reduction — directly actionable against RAGBrain's cold-query problem and known streaming/multi-agent limitations. (3) MCP at 97M installs with native OpenAI/Google/Salesforce support — now unambiguously infrastructure, not a trend to watch. (4) DeepSeek V4 (1T params, open-weights) and Microsoft Agent Framework RC (unified AutoGen + Semantic Kernel) are new signals this cycle. Cross-referencing against prior runs: the semantic cache (HIGH, March 30–31), Fast/Slow orchestration (HIGH, March 31), MCP integration (MEDIUM, March 31), and dependency pinning (HIGH→LOW, March 27) are all unconfirmed as implemented and are carried forward. Given the single-developer constraint and the stated priority of retrieval quality and latency over throughput, the semantic cache remains the single highest-ROI action available today.
### Recommendations
- [HIGH] **Retrieval — Semantic Cache**: Add an in-memory semantic cache (FAISS flat index) in front of the full retrieval pipeline. On each query, embed the question and check cosine similarity against cached (query_embedding → top-k results) pairs at a threshold of ~0.92. On a cache hit, skip Qdrant + BM25 + RRF + CrossEncoder entirely and return cached results. Use LRU eviction with a configurable TTL (e.g. 1 hour). Reference implementation: Salesforce VoiceAgentRAG (open-source, available today). This is a pure-additive, infrastructure-light change — no Qdrant migration required. *(Effort: DAYS)*
- [HIGH] **Retrieval — Knowledge Graph**: Introduce a lightweight knowledge graph layer alongside the existing vector + BM25 pipeline. Use spaCy or LLM-extracted entity/relation triples stored in NetworkX (in-process, no new infrastructure). At query time, extract entities from the query, traverse 1–2 hops in the graph, and inject the resulting context chunks into the RRF candidate set before reranking. This closes the 'no graph-based retrieval' known limitation. NetworkX is pure-Python and requires no new infra, keeping this compatible with the single-developer constraint. *(Effort: WEEKS)*
- [HIGH] **Agentic RAG — Orchestration / Streaming**: Introduce a Fast/Slow dual-agent split inside the existing LangGraph StateGraph. Fast path: semantic cache hit → stream answer directly (target <200ms). Slow path: full CRAG + Self-Reflective RAG pipeline for cache misses or low-confidence fast answers. Wire via asyncio.Queue. This simultaneously closes the 'no streaming' and 'no multi-agent coordination' known limitations and is directly modelled on the open-source VoiceAgentRAG architecture. Implement after the semantic cache (DAYS) is in place, as the Fast path depends on it. *(Effort: WEEKS)*
- [MEDIUM] **Ingestion / Integrations — MCP**: Expose RAGBrain's retrieval pipeline as an MCP tool server using the official Anthropic MCP Python SDK. This allows any MCP-compatible agent, IDE (Cursor, VS Code Copilot), or external orchestrator to query RAGBrain's knowledge base as a first-class tool. Secondarily, add an MCP client inside the CRAG query-rewriting node so RAGBrain can call external MCP servers (web search, filesystem, GitHub) during retrieval augmentation. The Anthropic SDK is already compatible with Claude Sonnet/Haiku which RAGBrain uses. *(Effort: DAYS)*
- [MEDIUM] **Embeddings — Model Upgrade**: Evaluate DeepSeek V4 (1T params, open-weights, released March 31) as a potential generation model alongside or replacing Claude Sonnet 4.6 for cost-sensitive or offline deployments. Do not migrate immediately — run a side-by-side benchmark on RAGBrain's existing eval set (if one exists) or a sample of 50 representative queries. Focus on answer quality, grounding accuracy (Self-Reflective RAG pass rate), and latency. If DeepSeek V4 matches Claude Sonnet on grounding at lower cost, offer it as an optional BYOK-style provider. *(Effort: DAYS)*
- [LOW] **Agentic RAG — Framework Watch**: Track the Microsoft Agent Framework (unified AutoGen + Semantic Kernel RC) as a potential future replacement for LangGraph. Do not migrate now. Actions: (a) read the RC release notes and map its graph workflow primitives to RAGBrain's current StateGraph nodes, (b) note features that LangGraph lacks and RAGBrain would benefit from (checkpointing, human-in-the-loop, native A2A, native MCP), (c) set a decision checkpoint for Q3 2026 when the framework reaches stable release. AutoGen vanilla moving to maintenance mode is a signal that the ecosystem is consolidating. *(Effort: DAYS)*
- [LOW] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies to exact versions in requirements.txt / pyproject.toml (anthropic, qdrant-client, langchain, sentence-transformers, faiss-cpu, spacy). Add pip-audit or safety to CI to alert on newly published CVEs. Consider hash-verified installs (pip install --require-hashes) for the critical dependency set. *(Effort: DAYS)*

### Already Covered
- ✓ Hybrid dense+sparse retrieval (RRF over Qdrant + BM25) — already implemented; cited as production baseline in March 31 briefing alongside knowledge graphs
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented; cited as production baseline
- ✓ Cross-encoder reranking post-RRF — already implemented; cited as production baseline
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ AST-based code chunking via Tree-sitter — already implemented
- ✓ LangGraph StateGraph orchestration — already implemented
- ✓ MCP conceptual coverage in knowledge base (The RLHF Book covers MCP as a JSON-RPC tool-use standard)

---

## Run: Saturday, April 04, 2026
### Summary
This run (April 4, 2026) introduces one major new signal not present in prior runs: Google DeepMind's published "AI Agent Traps" taxonomy (April 3, 2026), documenting six attack categories on agentic systems — including invisible HTML injection (86% success rate) and data exfiltration chains (100%). This is a named, documented threat class directly relevant to RAGBrain's WebExtractor and CRAG query-rewriting loop, both of which process untrusted external content. The $242B Q1 2026 AI investment figure and Slack's repositioning as a multi-app workflow orchestrator reinforce the agentic AI trajectory but don't change the technical roadmap. Against prior runs: the semantic cache (HIGH, carried since March 30), knowledge graph layer (HIGH, carried since March 31), Fast/Slow orchestration + streaming (HIGH, carried since March 31), and MCP integration (MEDIUM, carried since March 31) all remain unconfirmed as implemented and are carried forward with increased urgency. The new prompt-injection / agent-trap threat elevates the security recommendation from LOW back to MEDIUM — it is no longer just about dependency pinning but about active runtime attack surfaces in the ingestion and agentic loop. Given the single-developer constraint and the stated priority of retrieval quality and latency, the semantic cache remains the single highest-ROI action, followed by the knowledge graph layer and agent-trap hardening as the new entrant.
### Recommendations
- [HIGH] **Retrieval — Semantic Cache**: Add an in-memory semantic cache (FAISS flat index) in front of the full retrieval pipeline. On each query, embed the question and check cosine similarity against cached (query_embedding → top-k results) pairs at a threshold of ~0.92. On a cache hit, skip Qdrant + BM25 + RRF + CrossEncoder entirely and return cached results. Use LRU eviction with a configurable TTL (e.g. 1 hour). Reference implementation: Salesforce VoiceAgentRAG (open-source). This is a pure-additive, infrastructure-light change — no Qdrant migration required. *(Effort: DAYS)*
- [HIGH] **Retrieval — Knowledge Graph**: Introduce a lightweight knowledge graph layer alongside the existing vector + BM25 pipeline. Use spaCy or LLM-extracted entity/relation triples stored in NetworkX (in-process, no new infrastructure). At query time, extract entities from the query, traverse 1–2 hops in the graph, and inject the resulting context chunks into the RRF candidate set before reranking. This closes the 'no graph-based retrieval' known limitation. *(Effort: WEEKS)*
- [HIGH] **Security — Agent Trap Hardening**: Audit and harden the two primary attack surfaces identified in Google DeepMind's Agent Traps taxonomy: (1) WebExtractor — sanitise all fetched HTML before passing to the LLM; strip invisible/zero-width characters, hidden elements, and CSS-hidden text using a dedicated HTML sanitiser (e.g. bleach + custom rules) before trafilatura extraction. (2) CRAG query-rewriting node — add a prompt-injection guard that validates rewritten queries against a whitelist of expected query patterns and rejects rewrites containing instruction-like tokens (e.g. 'ignore previous', 'system:', base64 blobs). Log and alert on rejected rewrites. These are low-effort, high-impact mitigations against the documented 86% HTML injection and 100% exfiltration chain success rates. *(Effort: DAYS)*
- [HIGH] **Agentic RAG — Orchestration / Streaming**: Introduce a Fast/Slow dual-agent split inside the existing LangGraph StateGraph. Fast path: semantic cache hit → stream answer directly (target <200ms). Slow path: full CRAG + Self-Reflective RAG pipeline for cache misses or low-confidence fast answers. Wire via asyncio.Queue. Implement after the semantic cache is in place, as the Fast path depends on it. This simultaneously closes the 'no streaming' and 'no multi-agent coordination' known limitations. *(Effort: WEEKS)*
- [MEDIUM] **Ingestion / Integrations — MCP**: Expose RAGBrain's retrieval pipeline as an MCP tool server using the official Anthropic MCP Python SDK. This allows any MCP-compatible agent, IDE (Cursor, VS Code Copilot), or external orchestrator to query RAGBrain's knowledge base as a first-class tool. Secondarily, add an MCP client inside the CRAG query-rewriting node so RAGBrain can call external MCP servers (web search, filesystem, GitHub) during retrieval augmentation. The Anthropic SDK is already compatible with Claude Sonnet/Haiku which RAGBrain uses. *(Effort: DAYS)*
- [MEDIUM] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies to exact versions in requirements.txt / pyproject.toml (anthropic, qdrant-client, langchain, sentence-transformers, faiss-cpu, spacy, trafilatura). Add pip-audit or safety to CI to alert on newly published CVEs. Consider hash-verified installs (pip install --require-hashes) for the critical dependency set. This is separate from the agent-trap hardening above — it addresses supply-chain risk, not runtime injection. *(Effort: DAYS)*
- [LOW] **Agentic RAG — Framework Watch**: Track the Microsoft Agent Framework (unified AutoGen + Semantic Kernel RC) as a potential future replacement for LangGraph. Do not migrate now. Actions: (a) read the RC release notes and map its graph workflow primitives to RAGBrain's current StateGraph nodes, (b) note features that LangGraph lacks and RAGBrain would benefit from (checkpointing, human-in-the-loop, native A2A, native MCP), (c) set a decision checkpoint for Q3 2026 when the framework reaches stable release. *(Effort: DAYS)*

### Already Covered
- ✓ Hybrid dense+sparse retrieval (RRF over Qdrant + BM25) — already implemented; cited as production baseline
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented; cited as production baseline
- ✓ Cross-encoder reranking post-RRF — already implemented; cited as production baseline
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ AST-based code chunking via Tree-sitter — already implemented
- ✓ LangGraph StateGraph orchestration — already implemented
- ✓ MCP conceptual coverage in knowledge base (The RLHF Book covers MCP as a JSON-RPC tool-use standard)

---

## Run: Tuesday, April 14, 2026
### Summary
This run (April 14, 2026) has no new RSS signal — the feed source is misconfigured (RAGBRAIN_RSS_FEEDS_STR unset). The analysis therefore draws entirely on the accumulated prior-run state (March 27 – April 4, 2026) and the current ARCHITECTURE.md. Four items have now been carried as HIGH across five consecutive runs without confirmed implementation: (1) Semantic Cache, (2) Knowledge Graph layer, (3) Fast/Slow dual-agent orchestration + streaming, and (4) Agent Trap Hardening (elevated to HIGH on April 4). Their repeated deferral increases their urgency — each additional week without the semantic cache is a week of unnecessary cold-query latency on every request. The RSS feed misconfiguration is itself a new operational gap: the Architecture Review pipeline depends on live news ingestion and is currently blind. Given the single-developer constraint and the stated priority of retrieval quality and latency, the ranking this cycle is: fix the feed first (operational blocker), then semantic cache (highest-ROI latency win), then agent-trap hardening (active security risk with documented 86–100% attack success rates), then knowledge graph (retrieval quality), then Fast/Slow orchestration (depends on cache), then MCP (ecosystem integration), then dependency pinning (hygiene).
### Recommendations
- [HIGH] **Ingestion / Pipelines**: Fix the RSS feed misconfiguration: set RAGBRAIN_RSS_FEEDS_STR in .env with at least the following feeds — arXiv cs.IR (https://arxiv.org/rss/cs.IR), arXiv cs.CL (https://arxiv.org/rss/cs.CL), arXiv cs.CR (https://arxiv.org/rss/cs.CR), The Batch (https://www.deeplearning.ai/the-batch/feed/), and Import AI (https://jack-clark.net/feed/). Add a startup health-check that validates each feed URL returns HTTP 200 and logs a warning if zero articles are fetched in a 48-hour window. Without this, the Architecture Review pipeline is running blind and all future upgrade plans will lack fresh signal. *(Effort: DAYS)*
- [HIGH] **Retrieval — Semantic Cache**: Add an in-memory semantic cache (FAISS flat index, faiss-cpu) in front of the full retrieval pipeline. On each query: (1) embed the question using the existing all-mpnet-base-v2 model, (2) check cosine similarity against cached (query_embedding → top-k results) pairs at a threshold of ~0.92, (3) on a cache hit, skip Qdrant + BM25 + RRF + CrossEncoder entirely and return cached results directly. Use LRU eviction with a configurable TTL (default 1 hour, env-var override). Reference implementation: Salesforce VoiceAgentRAG (open-source). This is a pure-additive change — no Qdrant migration, no schema changes. Estimated implementation: ~150 lines of Python in a new retrieval/semantic_cache.py module. *(Effort: DAYS)*
- [HIGH] **Security — Agent Trap Hardening**: Harden the two primary attack surfaces identified in Google DeepMind's Agent Traps taxonomy (April 3, 2026): (1) WebExtractor — before passing fetched content to trafilatura, run it through a sanitisation step that strips invisible/zero-width Unicode characters (\u200b, \u00ad, etc.), CSS-hidden elements, and HTML comments using bleach + a custom allowlist. Add a unit test with a known HTML injection payload. (2) CRAG query-rewriting node — add a prompt-injection guard that validates LLM-rewritten queries against a pattern blocklist (e.g. 'ignore previous instructions', 'system:', base64-encoded blobs, URLs in query rewrites). Reject and log any rewrite that matches. These are low-effort mitigations against documented 86% HTML injection and 100% exfiltration chain success rates. *(Effort: DAYS)*
- [HIGH] **Retrieval — Knowledge Graph**: Introduce a lightweight knowledge graph layer alongside the existing vector + BM25 pipeline. During ingestion, use spaCy (en_core_web_sm) to extract named entity pairs and co-occurrence relations from each chunk, storing them as (head, relation, tail) triples in a NetworkX DiGraph persisted to disk as JSON. At query time: (1) extract entities from the query using spaCy, (2) traverse 1–2 hops in the graph to find related entity clusters, (3) look up the source chunk IDs for those entities, (4) inject those chunk IDs as a 'graph boost' into the RRF candidate set before CrossEncoder reranking. This closes the 'no graph-based retrieval' known limitation with zero new infrastructure (NetworkX is pure-Python). Start with the existing RSS/web corpus before extending to PDF books. *(Effort: WEEKS)*
- [HIGH] **Agentic RAG — Orchestration / Streaming**: Introduce a Fast/Slow dual-agent split inside the existing LangGraph StateGraph. Fast path: on semantic cache hit, stream the cached answer token-by-token via an asyncio.Queue (target end-to-end latency <200ms). Slow path: full CRAG + Self-Reflective RAG pipeline for cache misses, with streaming of the final generated answer using Claude's streaming API (anthropic.messages.stream). Wire the two paths with a conditional edge in the StateGraph: cache_hit → fast_stream_node, cache_miss → slow_crag_node → stream_node. This simultaneously closes the 'no streaming generation' and 'no multi-agent coordination' known limitations. Implement after the semantic cache (DAYS) is in place, as the Fast path depends on it. *(Effort: WEEKS)*
- [MEDIUM] **Ingestion / Integrations — MCP**: Expose RAGBrain's retrieval pipeline as an MCP tool server using the official Anthropic MCP Python SDK (mcp package). Implement two tools: (1) ragbrain_search(query: str, collection: str, top_k: int) → returns top-k chunks with scores, (2) ragbrain_ingest(url: str, collection: str) → triggers the web ingestion pipeline. This allows Cursor, VS Code Copilot, Claude Desktop, and any MCP-compatible orchestrator to query RAGBrain's knowledge base as a first-class tool. Secondarily, add an MCP client inside the CRAG query-rewriting node so RAGBrain can call external MCP servers (Brave Search, filesystem, GitHub) during retrieval augmentation when local results are insufficient. *(Effort: DAYS)*
- [MEDIUM] **Security / Dependency Management**: Pin all LLM-related PyPI dependencies to exact versions in requirements.txt or pyproject.toml: anthropic, qdrant-client, langchain, langchain-anthropic, langgraph, sentence-transformers, faiss-cpu, spacy, trafilatura, feedparser, slack-sdk, python-telegram-bot. Add pip-audit (pip install pip-audit && pip-audit) as a pre-commit hook and CI step. Consider hash-verified installs (pip install --require-hashes) for the critical subset. This is separate from the agent-trap hardening — it addresses supply-chain risk (cf. LiteLLM attack, March 27). *(Effort: DAYS)*
- [LOW] **Agentic RAG — Framework Watch**: Track the Microsoft Agent Framework (unified AutoGen + Semantic Kernel RC) as a potential future replacement for LangGraph. Actions this cycle: (a) read the RC release notes and map its graph workflow primitives to RAGBrain's current StateGraph nodes, (b) document any features LangGraph lacks that RAGBrain would benefit from (checkpointing, human-in-the-loop, native A2A, native MCP), (c) set a decision checkpoint for Q3 2026 when the framework reaches stable release. Do not migrate now. *(Effort: DAYS)*

### Already Covered
- ✓ Hybrid dense+sparse retrieval (RRF over Qdrant + BM25) — already implemented; cited as production baseline in multiple 2026 briefings
- ✓ Semantic chunking with cosine-distance topic boundaries — already implemented; cited as production baseline
- ✓ Cross-encoder reranking post-RRF (ms-marco-MiniLM-L-6-v2) — already implemented; cited as production baseline
- ✓ CRAG (Corrective RAG) with query rewriting — already implemented
- ✓ Self-Reflective RAG with hallucination checking — already implemented
- ✓ AST-based code chunking via Tree-sitter (6 grammars) — already implemented
- ✓ LangGraph StateGraph orchestration with conditional edges — already implemented
- ✓ MCP conceptual coverage in knowledge base (The RLHF Book covers MCP as a JSON-RPC tool-use standard)

---
